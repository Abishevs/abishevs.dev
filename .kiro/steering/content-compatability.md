# Content Compatibility

This project is a redesign of an existing Hugo website.

The existing content structure is authoritative.

The theme MUST adapt to the content.

The content MUST NOT be rewritten to fit the theme.

Avoid introducing breaking changes that require:

- rewriting existing posts
- rewriting project journals
- changing Org mode export
- changing frontmatter fields

Backwards compatibility is a primary requirement.
